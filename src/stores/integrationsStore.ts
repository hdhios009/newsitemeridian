/** Placeholder — integrations UI migrates in a later stage. */
export type IntegrationsState = Record<string, unknown>;

export const integrationsStore = {
  getSnapshot(): IntegrationsState {
    return {};
  },
};
